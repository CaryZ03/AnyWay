# 🔧 解决502错误 - 安全组配置指南

## 问题诊断

**问题原因**：云服务器安全组未开放18080端口，导致外部无法访问。

**不是代码问题！服务运行完全正常。**

---

## ✅ 已完成的修复

### 1. 服务器UFW防火墙

已成功开放18080端口：

```bash
sudo ufw allow 18080/tcp
sudo ufw reload
```

**状态**：✅ 完成

---

## ⚠️ 需要你操作：配置云服务器安全组

### 🔹 如果是**阿里云ECS**

1. 登录阿里云控制台
2. 进入 **云服务器 ECS** → **实例**
3. 找到你的服务器实例（IP: 106.12.174.161）
4. 点击 **更多** → **网络和安全组** → **安全组配置**
5. 点击 **配置规则** → **入方向** → **手动添加**

配置如下：

| 项目 | 值 |
|------|-----|
| 授权策略 | 允许 |
| 协议类型 | 自定义TCP |
| 端口范围 | 18080/18080 |
| 授权对象 | 0.0.0.0/0 |
| 描述 | AnyWay前端服务 |

6. 点击 **保存**

### 🔹 如果是**腾讯云CVM**

1. 登录腾讯云控制台
2. 进入 **云服务器** → **安全组**
3. 找到当前实例关联的安全组，点击 **修改规则**
4. 点击 **入站规则** → **添加规则**

配置如下：

| 类型 | 来源 | 协议端口 | 策略 |
|------|------|----------|------|
| 自定义 | 0.0.0.0/0 | TCP:18080 | 允许 |

5. 点击 **完成**

### 🔹 如果是**华为云ECS**

1. 登录华为云控制台
2. 进入 **弹性云服务器** → **安全组**
3. 找到实例所在的安全组，点击 **配置规则**
4. 点击 **入方向规则** → **添加规则**

配置如下：

| 协议 | 端口 | 源地址 | 说明 |
|------|------|--------|------|
| TCP | 18080 | 0.0.0.0/0 | AnyWay前端 |

5. 点击 **确定**

---

## 🧪 配置完成后测试

### 1. 从浏览器访问

打开浏览器，访问：
```
http://106.12.174.161:18080
```

应该可以看到前端页面！

### 2. 从服务器测试

```bash
curl -I http://106.12.174.161:18080
```

应该返回 `HTTP/1.1 200 OK`

---

## 📊 当前服务状态

### ✅ 服务运行正常

所有服务都在正常运行：

```bash
cd /root/pku_hlw/AnyWay
docker compose -f docker-compose.prod.yml ps
```

### ✅ 本地访问正常

```bash
curl http://localhost:18080
# 返回: HTTP/1.1 200 OK
```

### ✅ UFW防火墙已配置

```bash
sudo ufw status | grep 18080
# 18080/tcp    ALLOW    Anywhere
```

### ⏳ 待配置：云服务器安全组

这是导致外部无法访问的唯一原因。

---

## 🎯 配置后即可访问

一旦在云服务器控制台开放18080端口，你将可以立即访问：

- **前端页面**: http://106.12.174.161:18080
- **API文档**: http://106.12.174.161:18080/swagger/
- **管理后台**: http://106.12.174.161:18080/admin/

---

## 💡 常见问题

### Q: 为什么本地能访问但外部不能？
**A**: 云服务器有两层防火墙：
1. 系统防火墙(UFW) - ✅ 已配置
2. 云服务商安全组 - ⚠️ 需要你在控制台配置

### Q: 安全组配置后多久生效？
**A**: 通常立即生效，最多等待1-2分钟。

### Q: 0.0.0.0/0 安全吗？
**A**: 允许所有IP访问。如果需要限制，可以只允许特定IP段。

### Q: 如果还是502怎么办？
**A**: 
1. 等待1-2分钟让配置生效
2. 清除浏览器缓存
3. 检查云服务器控制台安全组规则是否正确保存
4. 联系云服务商技术支持

---

## 📞 需要帮助？

如果配置后仍无法访问，请提供：
1. 云服务商名称（阿里云/腾讯云/华为云等）
2. 安全组规则截图
3. curl测试结果

---

**提示**：这是云服务器的网络配置问题，与代码无关。服务本身运行完全正常！

