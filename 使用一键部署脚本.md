# 📦 使用一键部署脚本

如果你需要在新的服务器上部署，或者需要重新部署，可以使用一键部署脚本。

## 🚀 使用方法

```bash
cd /root/pku_hlw/AnyWay
./deploy.sh
```

## ✨ 脚本功能

该脚本会自动完成以下工作：

1. ✅ 检查系统环境（Ubuntu版本）
2. ✅ 安装Docker和Docker Compose（如未安装）
3. ✅ 配置Docker镜像加速器（使用国内镜像源）
4. ✅ 检查端口占用情况
5. ✅ 配置环境变量（自动生成安全密钥）
6. ✅ 创建必要的目录
7. ✅ 预拉取基础Docker镜像
8. ✅ 构建应用镜像
9. ✅ 启动所有服务
10. ✅ 初始化数据库
11. ✅ 创建管理员账号
12. ✅ 配置防火墙（可选）

## ⏱️ 预计时间

- **首次部署**: 10-20分钟（取决于网络速度）
- **后续部署**: 2-5分钟

## 🔧 脚本已做的优化

### 1. Docker镜像加速
使用国内镜像源，大幅提升下载速度：
- dockerproxy.com
- docker.registry.cyou
- docker-cf.registry.cyou
- dockercf.jsdelivr.fyi

### 2. 智能错误处理
- 自动检测并处理端口占用
- 构建失败自动重试
- 详细的错误日志

### 3. 安全配置
- 自动生成强密码
- 自动配置SECRET_KEY
- 自动配置ALLOWED_HOSTS

### 4. 交互式配置
- OpenAI API Key配置
- 防火墙规则配置
- 管理员账号创建

## 📝 脚本使用示例

### 完全自动化部署
```bash
cd /root/pku_hlw/AnyWay
./deploy.sh
```

按照提示操作即可。

### 查看脚本执行日志
脚本会将构建日志保存到 `build.log` 文件：
```bash
tail -f build.log
```

## ⚠️ 注意事项

1. **首次运行可能需要注销重新登录**
   - 如果脚本安装了Docker，需要注销后重新登录以使Docker组权限生效

2. **网络问题**
   - 如果镜像拉取失败，脚本会自动重试
   - 确保服务器可以访问互联网

3. **端口占用**
   - 脚本会检查端口占用并提示处理

4. **磁盘空间**
   - 确保至少有20GB可用空间

## 🔄 重新部署

如果需要重新部署：

```bash
cd /root/pku_hlw/AnyWay

# 停止并删除现有容器
docker compose -f docker-compose.prod.yml down -v

# 重新运行部署脚本
./deploy.sh
```

## 📊 部署后验证

脚本执行完成后，会显示：

```
=================================================================
               部署成功！
=================================================================

访问地址:
  前端页面:    http://your-ip:18080
  后端API:     http://your-ip:18080/api/v1/
  API文档:     http://your-ip:18080/swagger/
  管理后台:    http://your-ip:18080/admin/
```

## 🐛 故障排查

### 脚本执行失败

1. 查看错误日志
```bash
cat build.log
```

2. 检查Docker服务
```bash
sudo systemctl status docker
```

3. 检查磁盘空间
```bash
df -h
```

4. 手动重试构建
```bash
docker compose -f docker-compose.prod.yml build
```

### 镜像拉取失败

脚本已配置多个镜像源，如果仍然失败：

1. 检查网络连接
2. 检查DNS解析：`nslookup dockerproxy.com`
3. 手动配置镜像源：编辑 `/etc/docker/daemon.json`

## 📚 相关文档

- **当前部署状态**: [快速使用指南.md](./快速使用指南.md)
- **部署报告**: [部署成功报告.md](./部署成功报告.md)
- **详细文档**: [DEPLOYMENT.md](./DEPLOYMENT.md)

---

**提示**: 当前服务器已经成功部署，无需重新运行脚本。此脚本主要用于：
- 新服务器的首次部署
- 需要完全重新部署时
- 用作参考和学习部署流程

如果只需要更新代码，使用：
```bash
git pull
docker compose -f docker-compose.prod.yml up -d --build
```

