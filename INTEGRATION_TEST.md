# 前后端联调测试文档

## 测试环境
- 后端: http://localhost:8000
- 前端: http://localhost:5173

## API 测试

### 1. 测试后端 API 是否正常
```powershell
# 测试智能体列表接口
Invoke-WebRequest -Uri "http://localhost:8000/api/v1/agents/" -UseBasicParsing

# 预期响应:
# {
#   "code": 200,
#   "message": "获取智能体列表成功",
#   "data": [...],
#   "success": true
# }
```

### 2. 测试前端页面
访问 http://localhost:5173，应该看到:
- ✅ 页面标题: "项目开发"
- ✅ 搜索框和筛选器
- ✅ 智能体卡片列表
- ✅ 每个卡片显示: 图标、名称、描述、状态标签、用户信息、时间

### 3. 测试前端功能

#### 3.1 搜索功能
1. 在搜索框输入关键词
2. 列表应该实时过滤

#### 3.2 状态筛选
1. 点击状态下拉框
2. 选择"已发布"或"草稿"
3. 列表应该只显示对应状态的智能体

#### 3.3 数据加载
1. 刷新页面
2. 应该看到加载动画
3. 数据加载完成后显示智能体列表

## 验证清单

### 后端验证
- [ ] MySQL 容器运行正常
- [ ] Redis 容器运行正常
- [ ] Django 服务在 8000 端口运行
- [ ] API 返回正确的 JSON 格式
- [ ] CORS 配置允许前端访问

### 前端验证
- [ ] Vite 开发服务器运行正常
- [ ] 页面正常加载
- [ ] API 请求成功
- [ ] 数据正确显示
- [ ] 搜索和筛选功能正常
- [ ] 样式显示正确

## 常见问题

### 问题 1: 前端显示"连接失败"
**原因**: 后端服务未启动或 CORS 配置问题
**解决**:
```powershell
# 检查后端是否运行
netstat -an | findstr :8000

# 重启后端服务
cd backend
python manage.py runserver 0.0.0.0:8000
```

### 问题 2: 数据不显示
**原因**: 数据库中没有数据
**解决**:
```powershell
# 访问后端管理界面创建测试数据
# http://localhost:8000/admin/
```

### 问题 3: 样式显示异常
**原因**: CSS 文件未正确加载
**解决**:
```powershell
# 清除浏览器缓存并刷新
# 或者重启前端服务
cd frontend
npm run dev
```

## 成功标准
✅ 前端页面正常显示
✅ 能够看到智能体列表
✅ 搜索和筛选功能正常
✅ 数据实时更新
✅ 无控制台错误
