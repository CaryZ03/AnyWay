# 🚀 AnyWay AI Agent Platform - 快速使用指南

## 当前部署状态

✅ **服务已成功部署并运行！**

## 📍 访问地址

- **前端页面**: http://106.12.174.161:18080
- **API文档**: http://106.12.174.161:18080/swagger/
- **管理后台**: http://106.12.174.161:18080/admin/

## 🎯 立即开始使用

### 1. 创建管理员账号（首次使用）

```bash
cd /root/pku_hlw/AnyWay
docker exec -it aiagent-backend python manage.py createsuperuser
```

按提示输入：
- 用户名（例如：admin）
- 邮箱（可选，直接回车跳过）
- 密码（输入两次）

### 2. 配置OpenAI API Key（可选，启用AI功能）

```bash
# 编辑配置文件
nano .env

# 找到这一行并修改为你的真实API Key
OPENAI_API_KEY=sk-your-actual-openai-api-key

# 保存后重启服务
docker compose -f docker-compose.prod.yml restart backend celery-worker
```

### 3. 访问前端页面

在浏览器中打开：http://106.12.174.161:18080

## 📋 常用命令速查

### 查看服务状态
```bash
cd /root/pku_hlw/AnyWay
docker compose -f docker-compose.prod.yml ps
```

### 查看日志
```bash
# 实时查看所有日志
docker compose -f docker-compose.prod.yml logs -f

# 只看后端日志
docker logs -f aiagent-backend

# 只看前端日志
docker logs -f aiagent-frontend
```

### 重启服务
```bash
# 重启所有服务
docker compose -f docker-compose.prod.yml restart

# 重启后端
docker compose -f docker-compose.prod.yml restart backend

# 重启前端
docker compose -f docker-compose.prod.yml restart frontend
```

### 停止/启动服务
```bash
# 停止
docker compose -f docker-compose.prod.yml stop

# 启动
docker compose -f docker-compose.prod.yml start

# 完全删除（⚠️ 会删除数据）
docker compose -f docker-compose.prod.yml down -v
```

## 🔐 重要信息

### 数据库密码（已保存在.env文件中）
- MySQL Root: `Iqogaj5fE6oJmOsGQjN8uYi56sr0gVy`
- MySQL App: `obivBr108tSQMKPFjtu6vRKdl28NlnyN`

### 配置文件位置
- 环境变量：`/root/pku_hlw/AnyWay/.env`
- Docker配置：`/root/pku_hlw/AnyWay/docker-compose.prod.yml`

## 🛠️ 高级操作

### 进入容器内部
```bash
# 进入后端容器
docker exec -it aiagent-backend bash

# 进入数据库
docker exec -it aiagent-mysql mysql -u root -p
```

### 数据库操作
```bash
# 运行Django命令
docker exec aiagent-backend python manage.py <命令>

# 创建数据库迁移
docker exec aiagent-backend python manage.py makemigrations

# 应用迁移
docker exec aiagent-backend python manage.py migrate
```

### 备份数据
```bash
# 备份数据库
docker exec aiagent-mysql mysqldump -u root -p aiagent > backup.sql

# 恢复数据库
docker exec -i aiagent-mysql mysql -u root -p aiagent < backup.sql
```

## 🆘 遇到问题？

### 服务无法访问
1. 检查服务是否运行：`docker compose -f docker-compose.prod.yml ps`
2. 查看日志：`docker compose -f docker-compose.prod.yml logs`
3. 重启服务：`docker compose -f docker-compose.prod.yml restart`

### 防火墙问题
```bash
# Ubuntu UFW
sudo ufw allow 18080/tcp
sudo ufw status
```

### 云服务器安全组
确保在云服务商控制台开放了18080端口

## 📚 更多文档

- **部署成功报告**: [部署成功报告.md](./部署成功报告.md)
- **完整部署文档**: [DEPLOYMENT.md](./DEPLOYMENT.md)
- **快速部署指南**: [QUICKSTART.md](./QUICKSTART.md)
- **项目说明**: [README.md](./README.md)

## 🎓 下一步

1. ✅ 创建管理员账号
2. ✅ 登录管理后台
3. ✅ 配置OpenAI API Key（如需AI功能）
4. ✅ 创建你的第一个智能体
5. ✅ 开始使用！

---

**需要帮助？** 查看详细文档或联系技术支持。

**祝使用愉快！** 🎉

